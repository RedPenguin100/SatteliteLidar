name: mls_fixed
channels:
  - conda-forge
dependencies:
  - python=3.12
  - conda-build=25.1.2
  - numpy<2
  - numba=0.61
  - pytest
  - tqdm
  - scikit-learn=1.6.1
  - pandas=2.2.3
  - matplotlib=3.10.1
  - pip            # must list pip so conda installs it
  - pip:
    - finufft